# Today Menu

모바일 우선 메뉴 추천 서비스입니다.

- `frontend`: React + Vite + TypeScript
- `backend`: Node.js + Express + TypeScript, Ollama 및 Supabase 연동 예정

## 실행

```powershell
cd frontend
npm.cmd run dev
```

별도 터미널에서:

```powershell
cd backend
npm.cmd install
npm.cmd run dev
```